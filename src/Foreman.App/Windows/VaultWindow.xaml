<Window x:Class="Foreman.App.Windows.VaultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Foreman Agent Safety — Vault"
        Icon="/Resources/foreman.ico"
        Width="540" Height="640" MinWidth="460" MinHeight="420"
        Background="{StaticResource BackgroundBrush}"
        Foreground="{StaticResource TextPrimaryBrush}"
        FontFamily="Segoe UI, Arial"
        WindowStartupLocation="CenterScreen">
    <!-- Content is the shared VaultView, set in the constructor (it needs the VaultService). -->
</Window>
